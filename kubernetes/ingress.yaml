apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: leverage-analyzer-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: leverage-analyzer-service
            port:
              number: 80
